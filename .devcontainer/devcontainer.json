{
  "name": "IndangaScan",
  "image": "mcr.microsoft.com/devcontainers/python:3.12",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y tesseract-ocr && pip install -r requirements.txt && bash scripts/download_models.sh || true",
  "forwardPorts": [
    8000
  ]
}